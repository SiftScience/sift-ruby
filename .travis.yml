language: ruby
script: "bundle exec rake spec"
rvm:
  - 1.9.3
  - 2.0.0
  - 2.1.10
  - 2.2.5
  - 2.3.1
env:
  # None for now
gemfile:
  - Gemfile
services:
  # None for now
notifications:
  # None for now
